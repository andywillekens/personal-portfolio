services:
  app:
    build:
      dockerfile: Dockerfile
      context: ./frontend
    ports:
      - '3000:3000'
    volumes:
      - ./frontend:/frontend
      - ./frontend/node_modules:/frontend/node_modules
    command: >
      sh -c "npm i && npm run dev"
    restart: always
